// Default fetch limit value for infinite scroll components
export const DEFAULT_INFINITE_PREFETCH_LIMIT = 5;

// Default row name for the special Watch Later playlist each user will have upon creation
export const DEFAULT_WATCH_LATER_PLAYLIST_NAME = "Watch later"; //! This name should be maintained to match the definition in trigger.sql
export const WATCH_LATER_URL_KEYWORD = "watch-later"; // URL key for the watch later playlist
